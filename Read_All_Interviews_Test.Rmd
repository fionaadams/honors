---
title: 'Honors: Read In All'
output: html_document
---

Not sure why this isn't working!

https://cran.r-project.org/web/packages/readtext/vignettes/readtext_vignette.html

```{r}
require(readtext)
dat_word <- readtext(paste0("C:/Users/fionaadams/Documents/GitHub/honors/Transcripts", "/word/*.docx"))
```
```{r}
list_of_files <- list.files(path = "C:/Users/fionaadams/Documents/GitHub/honors/Transcripts", recursive = TRUE, pattern = "Transcript", full.names = TRUE)

library(data.table)

DT <- rbindlist( sapply(list_of_files, fread, simplify = FALSE),
                 use.names = TRUE, idcol = "FileName" )
```

```{r}
require(textreadr)
try2 <- as_transcript(sapply(listoffiles))
```
```{r}
path_data <- system.file("/Users/fionaadams/Documents/GitHub/honors/Transcripts", package = "readtext")
dat_word <- readtext(paste0(path_data,"/word/Transcripts/*.docx"))
```

